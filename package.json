{
  "name": "pequenos-pasos",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix app-pwa",
    "build": "npm run build --prefix app-pwa",
    "start": "npm run start --prefix app-pwa",
    "lint": "npm run lint --prefix app-pwa"
  }
}
