{
  "name": "estoyai-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Landing pública del apex estoyai.com — export estático always-on (Cloudflare Pages). Reusa la landing de app-pwa como único source of truth.",
  "scripts": {
    "build": "next build",
    "deploy": "next build && npx wrangler pages deploy out --project-name=estoyai-landing --branch=main --commit-dirty=true"
  },
  "dependencies": {
    "next": "15.1.7",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "typescript": "^5"
  }
}
